#!/bin/sh
# Python is installed on unbuntu, so nothing to do for python
# On unbuntu, 2.7.6 is installed, but this version does not contain pip
# so pip is installed below.
# FOR 2.7.6:  sudo apt-get install python-pip

# Install 2.7.9
# then::
#     python -m ensurepip
#     utiliser pip2.7
